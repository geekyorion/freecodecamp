// Example
function minusSeven(num) {
    return num - 7;
}

// Only change code below this line

// solution starts here ---------------------

function timesFive(num) {
    return num * 5;
}

console.log(timesFive(5));

// --------------------- solution ends here

console.log(minusSeven(10));

/**
 * A function takes some argument and perform some operation based on that arguments.
 * After completing the operation function should return the final answer or value to
 * the point where it is called.
 *
 * Here in the above we are calling the function "timesFive(5)" in "console.log()"
 * so it will return the value and it will be:
 *      console.log(timesFive(5)) => console.log(25) => "25"
 *
 * Tip: "return" can return only one value, if we want to return more than one value,
 *      then we can use an array or an object to wrap all of the data.
 */

// link: https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/basic-javascript/return-a-value-from-a-function-with-return